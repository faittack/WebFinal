using Microsoft.VisualBasic;

namespace Services
{
    public class Class1
    {
        public void Edit()
        {
            //var model= _context.Tabloİsmi.Find(vm.Id);
            //model.gereksinimleri=vm.gereksinimler;
            //_context.Entry(model).State=Entity.Modified;
            //_context.SaveChanges();
        }






    }
}